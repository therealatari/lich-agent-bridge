# Test fixtures

These are fabricated observations and controller metadata for deterministic
tests, not player records or recommended hunting configurations. The controller
fixture references fake scripts and is never installed as the runtime registry.

Keep new fixtures synthetic: use unrelated names, equipment, numeric values and
timestamps instead of copying a player's transcript and changing only the name.
